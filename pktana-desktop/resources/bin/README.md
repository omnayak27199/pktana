# Place the macOS `pktana` binary here before packaging:
#   cargo build --release --features pcap,tui
#   cp ../target/release/pktana bin/pktana
#
# CI (macos-latest) does this automatically.
.gitkeep
